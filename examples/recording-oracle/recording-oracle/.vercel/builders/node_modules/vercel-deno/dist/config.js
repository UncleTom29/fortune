"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.configString = exports.configBool = void 0;
const yn_1 = __importDefault(require("yn"));
function configBool(config, configName, env, envName) {
    const configVal = config[configName];
    if (typeof configVal === 'boolean') {
        return configVal;
    }
    if (typeof configVal === 'string' || typeof configVal === 'number') {
        const d = (0, yn_1.default)(configVal);
        if (typeof d === 'boolean') {
            return d;
        }
    }
    const envVal = env[envName];
    if (typeof envVal === 'string') {
        const d = (0, yn_1.default)(envVal);
        if (typeof d === 'boolean') {
            return d;
        }
    }
}
exports.configBool = configBool;
function configString(config, configName, env, envName) {
    const configVal = config[configName];
    if (typeof configVal === 'string') {
        return configVal;
    }
    const envVal = env[envName];
    if (typeof envVal === 'string') {
        return envVal;
    }
}
exports.configString = configString;
//# sourceMappingURL=config.js.map